<%= form_with(model: @note, local: true) do |form| %>

  <% if @note.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@note.errors.count, "error") %> prohibited
        this note from being saved:
      </h2>
      <ul>
        <% @note.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="card stickynoteShow stickynoteForm">
    <form>
      <%= form.label :author, class: "control-label" %>
      <div class="col-md-12">
        <%= form.text_field :author, class: "control-form" %>
      </div>

      <%= form.label :title, class: "control-label" %>
      <div class="col-md-12">
        <%= form.text_field :author, class: "control-form" %>
      </div>

      <%= form.label :category, class: "control-label" %>
      <div class="col-md-12">
        <%= form.text_field :author, class: "control-form" %>
      </div>

      <%= form.label :content, class: "control-label" %>

      <div class="col-md-12">
        <%= form.text_area :content, class: "control-form" %>
      </div>

      <%= form.submit "Save", class: "btn btn-success" %>
    </form>
  </div>
<% end %>
